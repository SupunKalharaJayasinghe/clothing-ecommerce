## Runtime
NODE_ENV=development
PORT=4000

## Database
# For local dev with Docker compose you can use:
# MONGO_URI=mongodb://root:example@mongo:27017/clothing?authSource=admin
# For Atlas/standalone replace with your connection string:
MONGO_URI=mongodb://localhost:27017/clothing

## CORS (frontend origin)
# When using Vite locally:
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
# When running behind nginx (docker-compose web service), this may be:
# CORS_ORIGIN=http://localhost

## Auth / Cookies
JWT_SECRET=change-me-in-prod
JWT_EXPIRES_IN=7d
COOKIE_SAMESITE=lax
COOKIE_SECURE=false

## Payments (optional; PayHere sandbox)
# PAYHERE_MERCHANT_ID=YOUR_MERCHANT_ID
# PAYHERE_RETURN_URL=http://localhost:5173/orders
# PAYHERE_CANCEL_URL=http://localhost:5173/checkout
# PAYHERE_NOTIFY_URL=http://localhost:4000/api/payments/payhere/webhook

## Email (optional; if not set in development, emails are logged to console)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# FROM_EMAIL=no-reply@example.com
