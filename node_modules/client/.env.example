# Development client env
# For local dev, the API runs on http://localhost:4000
VITE_API_BASE_URL=http://localhost:4000/api

# In Docker/production behind Nginx reverse proxy, we set this to a relative path
# VITE_API_BASE_URL=/api
